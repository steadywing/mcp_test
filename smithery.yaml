name: mcp-test
version: 1
description: "테스트입니다."

startCommand:
  type: stdio
  configSchema:
    type: object
    default: {}
    description: Start configuration for Calculator MCP Server
  commandFunction: |-
    (config) => ({
      command: "python",
      args: ["server_stdio.py"]
    })
  exampleConfig: {}

